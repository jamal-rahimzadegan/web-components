export const styles = `
<style>
* {
    user-select: none;
}

.controls-speed-meter-box {
    display: flex;
    align-items: center;
    justify-content: space-around;
}

.controls-cards-container {
    padding: var(--landing-inline-padding);
    display: flex;
    flex-wrap: wrap;
    align-items: flex-end;
    justify-content: space-between;
    margin-top: 20px;
}

</style>`
